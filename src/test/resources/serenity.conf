webdriver {
    driver = chrome

    timeouts {
        implicitlywait = 10000
    }
}

headless.mode = false

serenity {
    project.name = "T2"
    take.screenshots = AFTER_EACH_STEP
}

cucumber {
    features = "src/test/resources/features"
    glue = "sahe.com.steps"
}